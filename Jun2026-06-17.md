# TIL

## 날짜: 2026-06-18
## 소제목: Proxy, VPN, NAT, CIDR과 네트워크 모델 이해

### 새로 배운 내용

#### 주제 1: Proxy란 무엇인가
- Proxy는 클라이언트와 서버 사이에서 중계자 역할을 하는 네트워크 장치 또는 소프트웨어이다.
- 일반적인 통신은 클라이언트가 서버에 직접 요청하고, 서버가 클라이언트에게 직접 응답하는 구조이다.

```text
Client -> Server
```

- 하지만 실무에서는 중간에 프록시 서버를 두는 경우가 많다.

```text
Client -> Proxy -> Server
```

- Proxy는 대리인이라는 뜻이며, 네트워크에서는 클라이언트와 서버 사이에서 요청과 응답을 대신 전달하는 역할을 한다.
- 프록시가 없으면 구조는 단순하지만, 보안, 로깅, 캐싱, 접근 제어 같은 중간 처리를 하기 어렵다.
- 프록시가 있으면 클라이언트는 프록시에게 요청하고, 프록시가 서버에게 대신 요청한 뒤 응답을 다시 클라이언트에게 전달한다.

#### 주제 2: Proxy를 사용하는 이유
- Proxy는 보안 강화, 트래픽 최적화, 접근 제어, 로깅과 감시, 익명성, 정책 적용을 위해 사용한다.
- 클라이언트와 서버가 직접 연결되는 것을 줄이고, 중간에서 요청을 검사하거나 차단할 수 있다.
- 캐싱, 압축, 로드 밸런싱을 통해 성능을 개선할 수 있다.
- 허용된 사용자나 IP만 특정 서비스에 접근하도록 제한할 수 있다.
- 누가 어떤 요청을 했는지 로그로 남길 수 있다.
- 클라이언트의 실제 IP나 서버의 실제 IP를 숨기는 데도 사용할 수 있다.
- 회사나 학교 네트워크에서는 특정 사이트 접근 제한 같은 정책 적용에도 사용된다.

#### 주제 3: Forward Proxy
- Forward Proxy는 클라이언트 대신 외부 서버에 요청을 보내는 프록시이다.
- 클라이언트에게 가까이 있는 프록시라고 볼 수 있다.
- 클라이언트가 외부 서버에 직접 요청하지 않고 Forward Proxy에게 요청하면, Forward Proxy가 외부 서버에 대신 요청한다.
- 응답도 Forward Proxy가 받아 클라이언트에게 전달한다.

```text
Client -> Forward Proxy -> 외부 서버
```

- Forward Proxy의 핵심 문장은 "프록시야, 나 대신 example.com에 요청해줘"이다.
- 회사 인터넷 프록시, 학교 네트워크 프록시, 캐싱 프록시, 익명 프록시가 대표적인 예시이다.
- 기관에서는 보안 정책 적용과 사용 기록 관리를 위해 Forward Proxy를 사용할 수 있다.
- 개인 사용자는 익명성 확보나 제한 우회를 위해 사용할 수도 있다.

#### 주제 4: Reverse Proxy
- Reverse Proxy는 서버 대신 클라이언트 요청을 먼저 받아 처리하는 프록시이다.
- 클라이언트는 실제 백엔드 서버를 직접 알지 못하고, 서비스 주소 또는 Reverse Proxy에게 요청한다.
- Reverse Proxy는 요청을 받은 뒤 규칙 검사, 라우팅, 인증, 캐싱 등을 수행하고 정상 요청을 백엔드 서버로 전달한다.

```text
Client -> Reverse Proxy -> Backend Server
```

- 식당에서 손님이 주문을 직원에게 전달하고, 실제 음식은 주방에서 만드는 것과 비슷하다.
- Nginx, Apache HTTPD, Envoy, HAProxy, CloudFront, API Gateway 등이 Reverse Proxy 역할을 할 수 있다.
- 클라우드 개발자 입장에서는 Reverse Proxy가 특히 중요하다.

```text
Client
-> CloudFront / WAF
-> ALB / Nginx / API Gateway
-> Spring Boot / Node.js Server
```

#### 주제 5: Forward Proxy와 Reverse Proxy 비교

| 구분 | Forward Proxy | Reverse Proxy |
| --- | --- | --- |
| 누구의 대리자인가 | 클라이언트 | 서버 |
| 위치 | 클라이언트 쪽 | 서버 앞쪽 |
| 클라이언트가 아는 대상 | 프록시를 명시적으로 설정하는 경우가 많음 | 보통 서비스 주소만 알고 프록시는 의식하지 못함 |
| 서버가 보는 요청자 | 프록시 | 프록시 |
| 주요 목적 | 클라이언트 보호, 접근 제어, 우회, 익명성 | 서버 보호, 로드 밸런싱, SSL 처리, 캐싱 |
| 대표 예시 | 회사 인터넷 프록시, 캐싱 프록시 | Nginx, ALB, API Gateway, CDN |

- Forward Proxy는 클라이언트의 대리자이고, Reverse Proxy는 서버의 대리자이다.
- 같은 소프트웨어도 설정에 따라 Forward Proxy 또는 Reverse Proxy로 사용할 수 있다.
- Reverse Proxy와 Load Balancer는 완전히 같은 개념은 아니다.
- Reverse Proxy는 서버 앞에서 요청을 대신 받고 처리하는 중간자이고, Load Balancer는 여러 서버로 트래픽을 분산하는 기능이다.
- 실무에서는 하나의 장비나 서버가 Reverse Proxy와 Load Balancer 역할을 함께 수행하는 경우가 많다.

#### 주제 6: Reverse Proxy의 실무 역할
- Reverse Proxy는 요청 라우팅, 로드 밸런싱, SSL/TLS 처리, 서버 보호, 캐싱, 압축, 접근 제어, 로그 기록을 수행할 수 있다.
- `/api`는 API 서버로, `/static`은 정적 파일 서버로 전달하는 식으로 요청을 라우팅할 수 있다.
- 여러 서버 중 하나로 요청을 분산해 부하를 줄일 수 있다.
- HTTPS 인증서 처리를 Reverse Proxy에서 담당하면 백엔드 서버의 부담을 줄일 수 있다.
- 실제 서버 IP와 내부 구조를 외부에 숨겨 서버를 보호할 수 있다.
- 자주 요청되는 응답을 캐싱해 서버 부하를 줄일 수 있다.
- 요청 IP, 경로, 응답 코드, 처리 시간을 기록해 운영과 장애 분석에 활용할 수 있다.

#### 주제 7: VPN이란 무엇인가
- VPN(Virtual Private Network)은 공용 인터넷 위에 암호화된 가상 터널을 만들어, 멀리 떨어진 사용자나 네트워크를 사설망처럼 안전하게 연결하는 기술이다.
- 일반적으로 우리는 여러 라우터와 공유기를 거쳐 공용 네트워크를 통해 통신한다.
- 카페, 공항, 호텔 Wi-Fi처럼 보안성이 약할 수 있는 네트워크에서는 통신 내용을 보호할 필요가 있다.
- VPN은 인터넷 위에 별도의 암호화된 터널을 만든 것처럼 동작한다.
- 사용자의 실제 요청 내용을 한 번 더 감싸고 암호화하여 VPN 서버까지 전달한다.

#### 주제 8: VPN을 사용하는 이유
- VPN은 원격지에서 안전하게 내부망을 사용해야 할 때 사용한다.
- 회사 내부의 DB, Session Store, WAS, 사내 Git 서버는 외부에서 직접 접근할 수 없도록 막아두는 경우가 많다.
- 회사 내부 네트워크에서는 접근 가능하지만, 재택근무나 출장 중에는 외부 네트워크에 있기 때문에 VPN을 통해 내부망에 접속한다.
- VPN은 공용망에서 보안성을 강화하는 데도 사용된다.
- VPN을 사용하면 클라이언트는 VPN 서버와 통신하고, 내부의 실제 목적지 정보와 Payload는 터널 안에서 암호화된다.
- 떨어진 사설망들을 하나의 사설망처럼 묶을 때도 VPN을 사용할 수 있다.
- 예를 들어 본사와 지사, IDC와 Public Cloud VPC를 연결하는 구성에서 사용될 수 있다.

#### 주제 9: VPN 핵심 구성 요소와 동작 흐름
- VPN Client는 사용자의 PC나 스마트폰에서 VPN 연결을 시작하는 프로그램이다.
- VPN Server는 VPN 접속을 받아 인증하고 터널을 만들어주는 서버이다.
- VPN Tunnel은 클라이언트와 서버 사이의 암호화된 가상 통로이다.
- 인증 정보는 계정, 비밀번호, 인증서, MFA 등 사용자를 확인하기 위한 수단이다.
- 암호화 키는 데이터를 암호화하고 복호화하기 위한 키이다.
- 가상 IP는 VPN 연결 후 클라이언트에게 할당되는 내부망용 IP이다.
- 라우팅 정책은 어떤 트래픽을 VPN으로 보낼지 결정하는 설정이다.

VPN의 동작 흐름은 다음과 같다.

```text
1. VPN Client 실행
2. VPN Server로 연결 요청
3. 사용자 인증
4. 암호화 키 협상
5. VPN 터널 생성
6. 가상 IP 할당
7. 라우팅 테이블 변경
8. 트래픽 암호화 후 터널로 전송
9. VPN Server가 복호화 후 목적지로 전달
10. 응답도 다시 암호화되어 Client로 반환
```

#### 주제 10: 터널링과 VPN 연결 방식
- 터널링은 원본 패킷을 다른 프로토콜로 감싸서 보내는 방식이다.
- 원래 데이터를 VPN 프로토콜로 감싸고, 암호화된 터널로 전송한 뒤, VPN 서버에서 꺼내서 사용한다.
- 전용 통로를 만든 것처럼 공용 인터넷 위에 가상의 네트워크 경로를 만드는 것이다.
- VPN 연결 방식에는 Full Tunnel과 Split Tunnel이 있다.
- Full Tunnel은 모든 트래픽을 항상 VPN으로 보내는 방식이다.
- Split Tunnel은 사내망에 가야 하는 트래픽만 VPN으로 보내고, 나머지는 일반 인터넷으로 내보내는 방식이다.
- Full Tunnel은 보안 정책 적용이 강하지만 트래픽이 모두 VPN을 거쳐 성능 부담이 커질 수 있다.
- Split Tunnel은 효율적이지만 어떤 트래픽을 VPN으로 보낼지 라우팅 정책을 잘 설계해야 한다.

#### 주제 11: VPN 실무 관점과 주의점
- VPN은 재택근무자가 사내 Git, 운영 DB, 어드민 페이지, 내부 API에 접근할 때 사용될 수 있다.
- 운영 DB는 보통 Private Network에 있고 외부망과 직접 연결하지 않는다.
- 이런 경우 Bastion Server 또는 Jump Server와 함께 VPN을 사용해 접근 경로를 제한할 수 있다.
- VPN은 익명성을 어느 정도 제공할 수 있지만 완전한 익명성을 보장하지는 않는다.
- VPN 사업자는 사용자의 통신 내역을 볼 수 있으므로 신뢰할 수 있는 VPN을 선택해야 한다.
- VPN과 HTTPS는 같은 개념이 아니다.
- VPN은 장치 또는 네트워크 수준의 트래픽을 보호하고, HTTPS는 웹 브라우저와 웹 서버 사이의 HTTP 트래픽을 보호한다.
- VPN 안에서 HTTPS를 사용할 수도 있고, VPN 없이도 HTTPS를 사용할 수 있다.

#### 주제 12: NAT
- NAT(Network Address Translation)는 사설 IP와 공인 IP 주소를 변환하는 기술이다.
- IPv4 주소는 약 40억 개 정도로 제한되어 있고, 인터넷에 연결되는 장치 수는 계속 늘어났다.
- 모든 장치에 공인 IP를 하나씩 할당하기 어렵기 때문에 사설 IP와 NAT가 등장했다.
- 개인 PC나 스마트폰 같은 장치는 보통 사설 IP를 사용한다.
- 공유기나 NAT 장비는 내부 사설 IP와 외부 공인 IP 사이를 변환해 인터넷 통신을 가능하게 한다.
- 내부 장치가 서버로 요청을 보내면 NAT 장비는 출발지 사설 IP와 포트를 공인 IP와 포트로 변환한다.
- 응답이 돌아오면 NAT Table을 참고해 어느 내부 장치로 돌려보내야 하는지 판단한다.

#### 주제 13: NAT 주소 변환 방식
- NAT는 내부 사설 IP와 포트를 외부 공인 IP와 포트로 매핑한다.
- NAT Table은 이 매핑 정보를 저장하는 표이다.
- Symmetric NAT는 동일한 내부 IP와 포트를 사용해도 외부 대상에 따라 매핑 IP와 포트를 별도로 생성한다.
- Cone NAT는 외부 대상과의 관계에 따라 더 넓거나 제한적인 방식으로 외부 접근을 허용한다.
- Cone NAT에는 Full Cone NAT, Restricted Cone NAT, Port Restricted Cone NAT 등이 있다.
- NAT 방식은 P2P 통신, 실시간 통신, 게임, WebRTC 같은 환경에서 연결 성공 여부에 영향을 줄 수 있다.

#### 주제 14: 네트워크 프로토콜과 패킷
- 네트워크 프로토콜은 네트워크 상에서 데이터를 주고받는 규칙과 절차를 정의하는 체계이다.
- 네트워크 장치들이 같은 약속을 지켜야 데이터 전송이 원활하고 안전하게 이루어진다.
- HTTP, TCP, UDP, IP, ICMP, ARP 같은 것들이 프로토콜에 해당한다.
- 패킷은 네트워크를 통해 전송되는 데이터의 작은 단위이다.
- 데이터를 하나의 큰 덩어리로 보내는 대신 패킷 단위로 나누어 보내면 여러 경로를 통해 전송하고, 손실된 부분만 재전송하는 등의 처리가 가능하다.
- 네트워크 과정이 복잡해진 이유 중 하나는 데이터를 스트림처럼 통째로 보내는 것이 아니라 패킷 형태로 나누어 처리하기 때문이다.
- 실제 데이터는 개념 그림처럼 예쁘게 잘리는 것이 아니라 네트워크 계층과 MTU 같은 조건에 따라 블록 단위로 나뉘어 전송된다.

#### 주제 15: TCP와 UDP
- TCP와 UDP는 전송 계층에서 사용하는 대표적인 프로토콜이다.
- TCP는 연결 지향적이고 신뢰성 있는 전송을 제공한다.
- 데이터 순서 보장, 재전송, 흐름 제어, 혼잡 제어 같은 기능을 제공한다.
- 웹 요청, 파일 전송, 이메일처럼 정확성이 중요한 통신에 적합하다.
- UDP는 비연결 지향적이고 TCP보다 단순하며 빠르다.
- 순서 보장이나 재전송을 기본으로 제공하지 않는다.
- 실시간 스트리밍, 게임, 음성/영상 통화처럼 속도와 실시간성이 중요한 통신에 적합하다.
- TCP는 안정성을 중시하고, UDP는 속도와 지연 시간 감소를 중시한다고 정리할 수 있다.

#### 주제 16: CIDR
- CIDR(Classless Inter-Domain Routing)은 IP 주소 뒤에 `/숫자`를 붙여 이 IP가 어느 네트워크 범위에 속하는지 표현하는 방식이다.
- IP 주소는 네트워크 영역과 호스트 영역으로 나눌 수 있다.
- 네트워크 영역은 이 IP가 어느 네트워크에 속하는지를 나타낸다.
- 호스트 영역은 그 네트워크 안에서 몇 번 장치인지를 나타낸다.

```text
192.168.1.10/24

네트워크 영역: 192.168.1
호스트 영역: 10
```

- `/24`는 앞에서부터 24비트가 네트워크 영역이고, 나머지 8비트가 호스트 영역이라는 뜻이다.
- 라우터가 모든 개별 IP 주소의 위치를 하나하나 외우면 라우팅 테이블이 너무 커진다.
- CIDR을 사용하면 라우터는 `192.168.1.0/24`처럼 네트워크 대역 단위로 경로를 관리할 수 있다.

#### 주제 17: 클래스 방식과 CIDR 방식
- 초기 IP 주소 체계에서는 A, B, C 클래스처럼 고정된 크기로 네트워크를 나누었다.
- A Class는 `/8`, B Class는 `/16`, C Class는 `/24`에 해당한다.
- 하지만 고정된 클래스 방식은 실제 필요한 주소 수와 맞지 않아 주소 낭비가 발생했다.
- 예를 들어 장치가 500대 필요한 회사에 C Class는 부족하고, B Class는 너무 커서 많은 주소가 낭비된다.
- CIDR은 서브넷 마스크를 클래스에 고정하지 않고 필요한 크기만큼 네트워크 영역을 정할 수 있게 한다.
- 예를 들어 500개 정도의 주소가 필요하면 `192.168.0.0/23`처럼 더 적절한 범위를 사용할 수 있다.

#### 주제 18: CIDR과 호스트 수 계산
- CIDR 숫자가 클수록 네트워크를 더 잘게 쪼갠다는 뜻이다.
- CIDR 숫자가 커질수록 네트워크 비트가 많아지고, 호스트 비트가 줄어들어 사용할 수 있는 호스트 수가 줄어든다.
- 호스트 비트 수는 `32 - CIDR 프리픽스`로 계산한다.
- 사용 가능한 호스트 수는 일반적으로 `2^(호스트 비트 수) - 2`로 계산한다.
- 2를 빼는 이유는 네트워크 주소와 브로드캐스트 주소를 제외하기 때문이다.

```text
192.168.1.0/24
호스트 비트 수 = 32 - 24 = 8
사용 가능한 호스트 수 = 2^8 - 2 = 254개
```

| 구분 | 주소 |
| --- | --- |
| 네트워크 주소 | 192.168.1.0 |
| 사용 가능 첫 주소 | 192.168.1.1 |
| 사용 가능 마지막 주소 | 192.168.1.254 |
| 브로드캐스트 주소 | 192.168.1.255 |

#### 주제 19: AWS에서 CIDR 활용
- AWS 같은 클라우드에서는 VPC에 큰 CIDR 대역을 할당한 뒤, 이를 여러 Subnet으로 나누어 사용한다.
- 예를 들어 전체 서비스에 `10.0.0.0/16`을 할당하면 약 65,000개 규모의 주소 공간을 확보할 수 있다.
- 이후 Public Subnet과 Private Subnet으로 나누어 사용할 수 있다.

```text
VPC: 10.0.0.0/16

Public Subnet A: 10.0.1.0/24
Public Subnet B: 10.0.2.0/24
Private Subnet C: 10.0.3.0/24
Private Subnet D: 10.0.4.0/24
```

- Public Subnet에는 외부와 통신해야 하는 리소스를 두고, Private Subnet에는 DB나 내부 서버처럼 외부에 직접 노출하지 않을 리소스를 둘 수 있다.

### 오늘의 회고
- 전공 시간에 배워서 어느정도 알고 있는 내용이긴 했지만 이번 공부로 좀 더 확실히 이해하는 시간이 된 것 같다.
- 짧은 시간에 배우는 양이 많다 보니까 정리하고 공부할 시간이 촉박해지긴 하지만 습관을 잘 들여서 계속 열심히 해보자!
